# Build output.
/target
/dist

# Crawl and benchmark output: regenerated on demand, never reviewed.
/runs

# Issue tracker state: the maintainer's work queue, not part of the deliverable.
# ROADMAP.md is its public counterpart. No trailing slash, see the note below.
.beads
.bv

# Notes a tool writes about how a change was produced: which branch was merged into
# which, what a review round concluded. That is process, and this repository is read by
# strangers who need the software explained, not the work. No trailing slash, as above.
.kernl

# >>> repo-harness:gitignore-private-boundary >>>
# Maintainer notes — not part of the project; they live in the private notes repo,
# reachable through the `local` symlink. NO trailing slash on any path in this block:
# a worktree materializes these paths as SYMLINKS, and a `foo/` pattern matches a real
# directory only — the ignore then keeps working in the main tree and silently fails
# in every worktree.
/local

# Environment secrets and the per-machine agent sandbox config.
.env
.ai-jail
# <<< repo-harness:gitignore-private-boundary <<<
